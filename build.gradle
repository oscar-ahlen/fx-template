subprojects {
    apply plugin: 'java'
    apply plugin: 'idea'

    sourceCompatibility = '1.8'
    targetCompatibility = '1.8'

    repositories {
        jcenter()
        mavenLocal()
    }

    dependencies {
        compile 'log4j:log4j:1.2.17'

        testCompile 'junit:junit:4.+'
    }
}
